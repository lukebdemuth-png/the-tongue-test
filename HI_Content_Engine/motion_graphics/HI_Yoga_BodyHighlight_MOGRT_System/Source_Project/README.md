# Source Project Notes

This folder contains the builder script used to generate the branded `.mogrt` files in the `MOGRTS` folder.

The Premiere-ready MOGRTs are the primary deliverable.

The After Effects-authored motion internals for AE-based templates remain embedded inside their `.mogrt` packages as Adobe graphic archives, which Premiere reads through Essential Graphics.

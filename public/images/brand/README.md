# Brand Images

Current app name: `3-patterns`

Current logo:

- `3-patterns-logo.png`

Replacement rule:

Keep the same filename to replace the logo without changing code. If you use a new filename, update `siteConfig.logo` in `lib/site.ts`.

Notes:

- The header crops this image into a small square mark.
- The landing page also uses this file near the hero headline.
- Use a clean PNG or JPG with enough center padding so the mark reads at small sizes.
